x = 10;
y = 20;

% Equality
isEqual = (x == y);

% Inequality
isNotEqual = (x ~= y);

% Greater than
isGreater = (x > y);

% Less than
isLess = (x < y);

% Greater than or equal to
isGreaterEqual = (x >= y);

% Less than or equal to
isLessEqual = (x <= y);
